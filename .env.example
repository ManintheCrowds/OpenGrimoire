# Supabase Configuration (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional
NODE_ENV=development

# Set to 1 in local dev only to log Supabase URL + whether anon key is set (never logs the key).
# NEXT_PUBLIC_DEBUG_SUPABASE=1

# Set to 1 in local dev only for verbose visualization hook logs (never with real survey PII in shared builds).
# NEXT_PUBLIC_DEBUG_VISUALIZATION=1

# Brain map: server compares x-brain-map-key to BRAIN_MAP_SECRET. NEXT_PUBLIC_* variant is visible in client JS — see docs/security/NEXT_PUBLIC_AND_SECRETS.md
# BRAIN_MAP_SECRET=
# NEXT_PUBLIC_BRAIN_MAP_SECRET=

# Production (NODE_ENV=production): REQUIRED — route returns 503 if missing/blank. Dev: optional (open route; use localhost only).
# Callers send header x-alignment-context-key matching this value.
# ALIGNMENT_CONTEXT_API_SECRET=

# Optional: Redis for caching
# REDIS_URL=redis://localhost:6379

# Optional: Monitoring
# GRAFANA_URL=http://localhost:3000
# PROMETHEUS_URL=http://localhost:9090
