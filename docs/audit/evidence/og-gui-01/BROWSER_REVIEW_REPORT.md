# BrowserReviewReport — OG-GUI-01

**Date:** 2026-04-17  
**Base URL:** `http://localhost:3001`  
**Viewport:** 1280×720 (Playwright evidence spec)  
**Spec:** [gui-2026-04-16-opengrimoire-survey.md](../gui-2026-04-16-opengrimoire-survey.md) § BrowserReviewSpec

## Flow results

| Flow | Result | Notes |
|------|--------|--------|
| 1. `/operator-intake` → full Sync Session submit | **PASS** | `POST /api/survey` 200; success step visible. Screenshot: `01-operator-intake-after-submit.png`. |
| 2. `/survey` → `/operator-intake` | **PASS** | URL matches `/operator-intake` within 15s. Screenshot: `02-survey-redirect-operator-intake.png`. |
| 3. `/login` → `/admin` moderation panel | **PASS** | Operator login; heading **Response Moderation Queue** visible. Screenshot: `03-admin-moderation-panel.png`. Row-level UI assertions remain **OG-GUI-02**. |

## Evidence

| Artifact | Path (repo-relative from `OpenGrimoire/`) |
|----------|---------------------------------------------|
| Playwright HTML report | `playwright-report/index.html` (regenerated by `npx playwright test …`) |
| Core E2E (survey + admin API) | `npx playwright test e2e/survey.spec.ts e2e/admin-moderation.spec.ts` — **7 passed**, **2 skipped** (token-gate) |
| OG-GUI-01 screenshots + console/network JSON | `docs/audit/evidence/og-gui-01/*.png`, `01-console-network.json`, `02-console-network.json`, `03-console-network.json` |
| Evidence automation | `e2e/og-gui-01-browser-review-evidence.spec.ts` |
| Verify bundle | `npm run verify` — exit **0** (2026-04-17; ESLint warnings only, no errors) |

## Console (summary)

- **Flows 1–2:** Browser-reported errors are **404** on branding font URLs under `/src/branding/*.woff2` (cosmetic; fonts may load via other paths or fall back).
- **Flow 3:** No console **error**-level messages captured after login.

## Failed network (summary)

- **Flows 1–2:** `net::ERR_ABORTED` on the same `.woff2` URLs (paired with 404 resource messages).
- **Flow 3:** `net::ERR_ABORTED` on `admin?_rsc=…` RSC requests — common during Next.js App Router navigation when a flight is superseded; **not** treated as an app failure (page reached stable moderation heading).

## Blockers

None for OG-GUI-01. Use **credential vault / env** for non-default `OPENGRIMOIRE_ADMIN_PASSWORD`; do not commit secrets.
