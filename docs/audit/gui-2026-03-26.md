# GUI audit — OpenGrimoire

**Portfolio index (sibling repo audits):** [MiscRepos `GUI_AUDIT_PORTFOLIO_INDEX.md`](../../../MiscRepos/docs/audit/GUI_AUDIT_PORTFOLIO_INDEX.md).

**Date:** 2026-03-26  
**Mode:** Read-only audit  
**Stack:** Next.js 14 App Router, React 18, Tailwind, D3, Three.js, Zustand; **local SQLite** for Sync Session + alignment (no hosted DB).  
**Dev URL:** `http://localhost:3001` (`package.json` `dev` script).

**Portfolio GUI ritual (MiscRepos harness):** With MiscRepos cloned as a **sibling** of this repo, onboarding checklist: `../MiscRepos/docs/audit/NEW_GUI_SURFACE_CHECKLIST.md`. Two-layer contract: `../MiscRepos/docs/audit/GUI_WAVE_TWO_LAYER_CONTRACT.md`. **OpenGrimoire-focused weekly prompt** (wave + OA-* ids + one agent-native principle):

```bash
cd ../MiscRepos
python .cursor/scripts/weekly_gui_wave_prompt.py --config docs/audit/gui_wave_rollout_opengrimoire.yaml
```

Paste the output into chat, then the **fixed session contract** from `GUI_WAVE_TWO_LAYER_CONTRACT.md` §2 (Goal, in-scope files, out-of-scope, `npm run test:e2e`, README/CI Playwright). Default `gui_wave_rollout.yaml` in MiscRepos remains for **moltbook_watchtower**; this repo uses `gui_wave_rollout_opengrimoire.yaml`.

## Severity rubric (summary)

- **P0** — Broken primary journey, security, data loss, crash loop  
- **P1** — Major flow blocked; severe a11y; persistent console errors on happy path  
- **P2** — Confusing UX; moderate a11y; layout break at one breakpoint  
- **P3** — Polish, copy, minor drift  

---

## 1. Route / page inventory

| Route | Shell / layout | Main UI | Data source |
|-------|----------------|---------|-------------|
| `/` | Root `layout.tsx` — `ClientProviders`, `SharedNavBar`, `main`, `SiteFooter` | Home cards (`src/app/page.tsx`) | Static links |
| `/context-atlas` | Root layout | Brain map / context graph (`src/app/context-atlas/page.tsx` + `BrainMap/*`) | `GET /api/brain-map/graph`, static JSON fallbacks |
| `/brain-map` | Root layout | Same viewer pattern as context atlas (`src/app/brain-map/page.tsx`) | Same as above |
| `/visualization` | Root layout | D3 demos | Local/sample data adapters |
| `/visualization/alluvial`, `/visualization/dark` | Segment `layout.tsx` | Themed alluvial views | Local |
| `/operator-intake` | Root layout | Sync Session form (`sync-session-form-container`) | SQLite via `POST /api/survey` |
| `/survey` | Root layout | Legacy alias (same Sync Session UI) | SQLite via `POST /api/survey` |
| `/login` | `login/layout.tsx` | Operator auth | Session cookie + env password |
| `/admin`, `/admin/controls`, `/admin/alignment` | Root layout | Admin surfaces | Env / secrets |
| `/capabilities` | Root layout | Capabilities UI | `GET /api/capabilities` |
| `/constellation` | `constellation/layout.tsx` | 3D constellation | Local / APIs |
| `/test`, `/test-chord`, `/test-context`, `/test-sqlite` | Various test layouts | Dev/test pages | Local fixtures; `/test-supabase` redirects to `/test-sqlite` |

**Global providers:** `src/app/providers.tsx` (via `ClientProviders` in `src/app/layout.tsx` lines 22–28).

**Nav vs home:** `SharedNavBar` includes **Operator intake** (2026-03-30, OA-2). Home grid still offers `home-card-operator-intake` for parity; smoke covers both paths (`e2e/smoke.spec.ts`).

---

## 2. Critical user journeys (3–7)

1. **Land → open context graph** — `/` or nav → `/context-atlas` → graph or empty/placeholder. APIs: `GET /api/brain-map/graph`.
2. **Land → visualization** — Home card or nav → `/visualization` → D3 mount. Mostly client-side.
3. **Operator intake** — Home card → `/operator-intake` → multi-step form → submit → SQLite persistence.
4. **Admin controls** — Nav → `/admin/controls` → global visualization settings.
5. **Capabilities discovery** — Nav → `/capabilities`; API check `GET /api/capabilities` (`e2e/capabilities.spec.ts`).

---

## 3. Static pass (code-level)

- **Error / loading:** Context atlas specs expect loading copy and empty states (`e2e/context-atlas.spec.ts`); brain map components should expose test ids (`brain-map-graph`, `brain-map-placeholder-hint`).
- **a11y (target WCAG AA):** Root layout sets `lang="en"`. `SharedNavBar` uses `aria-label="Main navigation"`. **OA-6 (2026-03-30):** Playwright regression for Brain Map **tablist**, Graph/Table **tabs**, layer tabs, **Space** keyboard activation, SVG `aria-label`, and Refresh button (`e2e/brain-map-a11y-oa6.spec.ts`). Optional manual SR spot-check (NVDA/JAWS/VoiceOver) remains for full AT confidence — see §10.
- **i18n:** English-only strings in audited paths.

---

## 4. Visual & responsive

- Tailwind breakpoints (`sm`/`md`/`lg`) used on home grid (`src/app/page.tsx` `grid-cols-1 md:grid-cols-3`).
- **OA-7 (2026-04-03):** `SharedNavBar` wraps on narrow widths; `context-atlas` uses flex/`min-h-0` instead of `100vw`/`100vh` shell overflow; Brain Map toolbar stacks controls with **44px** touch targets on small screens; `/visualization` uses a bounded-height container instead of `fixed` full-screen. Automated checks: [`e2e/responsive-oa7.spec.ts`](../e2e/responsive-oa7.spec.ts) at **375×667** and **720×800** (`documentElement` horizontal overflow guard).

---

## 5. Runtime pass

- **Automated:** Playwright was run 2026-03-26 (see §7).  
- **Manual:** Use Browser Ready pattern for `/context-atlas` (wait for loading to clear), Three.js/D3-heavy routes, and Sync Session flows with real SQLite env when needed.

---

## 6. E2E / automation gap map

| Journey | Coverage | Test file |
|---------|----------|-----------|
| Home heading | Covered | `e2e/smoke.spec.ts` |
| Nav + visualization + intake + admin (combined) | Covered | `e2e/smoke.spec.ts` — see §7 |
| Context atlas load / heading / mocked graph / empty | Covered | `e2e/context-atlas.spec.ts` |
| Visualization SVG/canvas | Covered | `e2e/visualization.spec.ts` |
| Sync Session multi-step + prev/next | Covered | `e2e/survey.spec.ts` |
| `/api/capabilities` | Covered | `e2e/capabilities.spec.ts` |
| `/brain-map` alias | Covered | `e2e/brain-map.spec.ts` — redirect to `/context-atlas` + graph shell parity |
| `/login`, `/admin/alignment`, `/constellation` | Covered | `e2e/auth-alignment-constellation.spec.ts` (OA-5) |
| Brain Map tabs + graph shell (OA-6) | Covered | `e2e/brain-map-a11y-oa6.spec.ts` — mocked graph; tablist, keyboard, layer tabs |
| `/test*` dev/demo routes | Gated in prod (`middleware`); smoke in dev for `/test`, `/test-chord`, `/test-context`, `/test-sqlite` + redirect from `/test-supabase` | `e2e/test-routes.spec.ts` (keep aligned with `middleware.ts` matchers), `.env.example` |
| Narrow viewport / overflow (OA-7) | Covered | `e2e/responsive-oa7.spec.ts` — `/context-atlas` + `/visualization` at 375px and 720px |
| Maestro mobile smoke | Optional — run on release branches | `npm run test:maestro` (`e2e/maestro/smoke_web.yaml`); see [`RELEASING.md`](../../RELEASING.md) |

---

## 7. Verification evidence (commands)

```bash
npm run test:e2e
```

**Result (2026-03-26 initial):** 10 passed, 1 failed (smoke nav). **Result (2026-03-26 follow-up):** smoke fixed — use `Promise.all([page.waitForURL(...), page.getByTestId(...).click()])` for client-side `next/link` navigation in `e2e/smoke.spec.ts`. **Result (2026-03-30):** OA-2 — Operator intake added to `SharedNavBar`; smoke asserts nav from `/context-atlas`. **Result (2026-03-30 OA-3):** `e2e/brain-map.spec.ts` — `/brain-map` redirect + parity with context-atlas graph shell. **Result (2026-03-30 OA-4):** `middleware.ts` gates `/test*` in production; `OPENGRIMOIRE_ALLOW_TEST_ROUTES` in `.env.example`; `e2e/test-routes.spec.ts` (dev server).

- **Resolved:** `e2e/smoke.spec.ts` — nav link clicks now await URL in parallel with click (hydration-safe).

**Result (2026-03-30 OA-6):** `e2e/brain-map-a11y-oa6.spec.ts` — automated tablist/keyboard/layer/refresh checks; full Playwright suite **27 passed** (`npm run test:e2e`).

**Result (2026-04-03 OA-7):** `e2e/responsive-oa7.spec.ts` — responsive regression for `/context-atlas` and `/visualization`; run `npm run test:e2e` after UI layout changes (adds **+3** tests vs prior suite count).

---

## 8. Issue list (prioritized)

| ID | Sev | Summary | Evidence |
|----|-----|---------|----------|
| OA-1 | — | ~~Smoke test nav flake~~ **Fixed** (2026-03-26): `waitForURL` + `click` parallel | `e2e/smoke.spec.ts` |
| OA-2 | — | ~~Operator intake absent from top nav~~ **Fixed** (2026-03-30): nav link + `nav-link-operator-intake`; home card `home-card-operator-intake` | `SharedNavBar.tsx`, `page.tsx`, `e2e/smoke.spec.ts` |
| OA-3 | — | ~~`/brain-map` not in E2E matrix~~ **Fixed** (2026-03-30): redirect + graph/heading parity | `e2e/brain-map.spec.ts`, `src/app/brain-map/page.tsx` |
| OA-4 | — | ~~Test/demo `/test*` routes~~ **Fixed** (2026-03-30): production 404 unless `OPENGRIMOIRE_ALLOW_TEST_ROUTES`; dev unchanged | `middleware.ts`, `.env.example`, `e2e/test-routes.spec.ts` |
| OA-5 | — | ~~E2E gap: `/login`, `/admin/alignment`, `/constellation`~~ **Fixed** (2026-03-30): Playwright smoke uses `OPENGRIMOIRE_ADMIN_PASSWORD` from `playwright.config.ts` webServer | `e2e/auth-alignment-constellation.spec.ts` |
| OA-6 | P3 | ~~Brain Map tabs + graph controls a11y~~ **Fixed** (2026-03-30): E2E regression + optional manual SR follow-up in §10 | `e2e/brain-map-a11y-oa6.spec.ts`, §10 |
| OA-7 | P3 | ~~Narrow viewport overflow / touch targets~~ **Fixed** (2026-04-03): nav wrap, context-atlas flex shell, Brain Map toolbar stack, visualization bounded height; Playwright overflow guard | `e2e/responsive-oa7.spec.ts`, `SharedNavBar.tsx`, `BrainMapGraph.tsx`, `context-atlas/page.tsx`, `visualization/page.tsx` |

---

## 9. Backlog (next actions)

1. **Optional:** Manual SR spot-check (NVDA / JAWS / VoiceOver) on `/context-atlas` if release policy requires full AT sign-off — checklist in §10.  
2. **Release cadence:** Maestro — see [`RELEASING.md`](../../RELEASING.md).  
3. Optional: `npm run verify` before merge (lint + type-check + unit tests).

---

## 10. OA-6 evidence (automated + optional manual)

**Automated (CI):** [`e2e/brain-map-a11y-oa6.spec.ts`](../e2e/brain-map-a11y-oa6.spec.ts) exercises mocked `GET /api/brain-map/graph` data and asserts:

- `role="tablist"` with accessible name **View mode**; Graph and Table **tabs** with `aria-selected` and linked **tabpanels**.
- **Space** key on the Table tab moves focus to the table panel and shows the data table.
- Layer filter tabs (**All** / **State** / **Vault**) toggle `aria-selected`.
- **Refresh graph** button exposes a descriptive `aria-label`.
- Graph **SVG** exposes `aria-label="Context graph visualization"`.

**Optional manual (operator):** On a non-production build, open `/context-atlas`, confirm tab order and screen reader announcements match expectations for your AT; record pass/fail and date below when required by release policy.

| Check | Date | Result |
|-------|------|--------|
| Optional AT spot-check | — | Not required for OA-6 closure (automated coverage sufficient for wave) |

---

## Definition of done (this artifact)

- [x] Route inventory with data sources  
- [x] Journeys listed  
- [x] Static + responsive notes  
- [x] E2E gap map  
- [x] Findings with file/test references  
- [x] Command evidence for E2E  
