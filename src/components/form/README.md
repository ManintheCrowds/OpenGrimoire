# `components/form`

Reserved for **future** form components. The shipped **intake** survey lives under [`../SurveyForm`](../SurveyForm/) with [`useSurveyForm`](../../lib/hooks/useSurveyForm.ts).

A separate **async HITL intent** survey (AI-posted questions for humans) is **backlog** only — see [`docs/HITL_INTENT_SURVEY_BACKLOG.md`](../../../docs/HITL_INTENT_SURVEY_BACKLOG.md).

The previous demo file `SurveyForm.tsx` in this folder was removed as unused; routes use `components/SurveyForm` only.
