# `components/form`

Reserved for **future** form components. The shipped **Sync Session** form lives under [`../SyncSessionForm`](../SyncSessionForm/) with [`useSyncSessionForm`](../../lib/hooks/useSyncSessionForm.ts).

A separate **async HITL intent** survey (AI-posted questions for humans) is **backlog** only — see [`docs/HITL_INTENT_SURVEY_BACKLOG.md`](../../../docs/HITL_INTENT_SURVEY_BACKLOG.md).

The previous demo file `SurveyForm.tsx` in this folder was removed as unused; routes use `components/SyncSessionForm` only.
